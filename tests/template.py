import pytest
import projects
import projects.db as db
from projects.models import Project

class TestEditProject: # TODO: make work with get projects when possible
    def test_edit_project_title(self):
        pass